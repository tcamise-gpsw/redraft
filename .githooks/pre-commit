#!/usr/bin/env sh
set -e

# Run from the repo root so npm scripts resolve correctly.
cd "$(git rev-parse --show-toplevel)"

echo "pre-commit: format check..."
npm run format:check --silent

echo "pre-commit: lint..."
npm run lint --silent

echo "pre-commit: done."
